NODE_ENV=development
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Server-only API keys and secrets
FINNHUB_API_KEY=
MONGODB_URI=
BETTER_AUTH_SECRET=
# Optional only for localhost development: app falls back to a fixed dev-only secret when this is empty.
BETTER_AUTH_URL=http://localhost:3000
GEMINI_API_KEY=
NODEMAILER_EMAIL=
NODEMAILER_PASSWORD=
